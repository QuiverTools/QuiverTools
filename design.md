```
class Quiver
  # maybe we could choose the basis to use?
  def cartan_matrix()
  def euler_matrix()
  def coxeter_matrix()

  def harder_narasimhan()
    # need to be able to easily determine codimension of a stratum

  def harder_narasimhan_polygon()
    # Definition 3.6

  def codimension(d)
    # codimension of a Harder-Narasimhan stratum

  def is_semistable(d)
    # Corollary 3.5 of Reineke's Inventiones paper

# not sure whether to separate this out: maybe useful for interfacing?
class PathAlgebra
```
